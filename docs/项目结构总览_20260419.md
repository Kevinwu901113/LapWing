# Lapwing 项目结构总览 (2026-04-19)

> 对 `/home/kevin/lapwing` 做的一次完整盘点。结构按"**运行时 → 数据 → 前端 → 工具/测试 → 文档**"逐层展开；每一节都会标注 **✅ 活跃 / ⚠ 过渡 / 🚧 实验 / 🗑 废弃**。
>
> 重要前提：**当前 `CLAUDE.md` 与代码现状已有偏差**（遗漏了 v2.0 重构新增的模块，同时仍保留若干已被删除的旧词汇）。本文以磁盘真实文件为准。

---

## 0. 顶层目录

```
/home/kevin/lapwing/
├── main.py                    ✅ CLI 入口 + Bot 启动
├── requirements.txt           ✅
├── pytest.ini                 ✅ asyncio_mode=auto
├── CLAUDE.md                  ⚠ 部分章节过期（见附录 A）
├── README.md                  ✅
├── Lapwing_角色设定书.md        ✅ 角色设定参考
├── Lapwing_项目状况_20260416.md ⚠ Step 1 之后已稍滞后
├── HEALTH_CHECK_REPORT.md     ⚠ 审计报告（部分项已修复）
├── cleanup_report_step1.md    ✅ Step 1 完成记录
├── cleanup_report_step2.md    ✅ Step 2 完成记录
├── cleanup_report_step3.md    ✅ Step 3 完成记录
├── src/                       ✅ 后端 Python 源码
├── desktop-v2/                ✅ 活跃前端 (Tauri v2 / React 19)
├── desktop/                   🗑 旧前端 (Tauri v1)，保留兼容但不再维护
├── tests/                     ✅ 1072 条，100% 通过
├── prompts/                   ✅ 提示词（热重载）
├── data/                      ✅ 运行时数据
├── config/                    ✅
├── scripts/                   ✅ 部署 / 诊断 / 迁移脚本
├── skills/                    🚧 经验技能库（实验期）
├── skill_traces/              ⚠ 技能执行 trace 归档（只存不读）
├── docs/                      ✅ 设计文档 + 重构日志
├── docker/                    ⚠ sandbox 容器（辅助用）
├── watchdog/                  ✅ systemd 守护进程
├── logs/                      ✅ 运行日志（lapwing.log 等）
└── venv/, __pycache__/        — 环境 / 字节码
```

---

## 1. Python 后端：`src/`

### 1.1 `src/app/` — 应用装配

| 文件 | 作用 | 状态 |
|------|------|------|
| `container.py` | **DI 根容器**。装配 Brain / MainLoop / EventQueue / ChannelManager / Dispatcher / DurableScheduler / TrajectoryStore / CommitmentStore / Attention / LocalApiServer | ✅ |
| `task_view.py` | 对外暴露任务视图（给 `/api/v2/life/timeline`） | ✅ |

> 注意：CLAUDE.md 提到的 `TelegramApp` 已被移除，当前只剩 QQ + Desktop 两条通道。

### 1.2 `src/core/` — 核心运行时

分组列出（按功能聚类），所有文件都在 `src/core/` 下。

**对话主路径**
- `brain.py` ✅ — `LapwingBrain`，所有入口最终进这里
- `task_runtime.py` ✅ — 工具循环（LLM ↔ tool，带 loop detection / error budget / shell policy）
- `llm_router.py` ✅ — 按"槽位"(chat/tool/heartbeat/browser_vision) 路由到不同模型
- `llm_protocols.py` ✅ — OpenAI / Anthropic / NIM 协议适配
- `llm_types.py` / `llm_exceptions.py` ✅
- `task_types.py` / `task_model.py` ✅ — 任务运行时数据结构
- `reasoning_tags.py` ✅ — 处理 `<internal_thinking>` / `<user_visible>`
- `output_sanitizer.py` ✅（极薄，旧的 auto-send 已删）

**Blueprint v2.0 新加的"单消费者"模型（Step 4+）**
- `main_loop.py` ⚠ 骨架已搭，M2/M3/M4 handler 仍是 TODO
- `event_queue.py` ✅ — 带优先级 peek 的 asyncio 队列
- `events.py` ✅ — `MessageEvent` / `InnerTickEvent` / `SystemEvent`
- `inner_tick_scheduler.py` ✅ — 自发意识节拍（空闲时 backoff）
- `attention.py` ✅ — "现在在跟谁说话 / 在做事吗"
- `dispatcher.py` ⚠ 内存 pub/sub，**过渡态**：逐步被 mutation_log 取代
- `trajectory_store.py` ✅ — 跨频道只追加的行为时间线
- `commitments.py` ⚠ 结构已在，Step 5 reviewer 还没落地
- `durable_scheduler.py` ✅ — 持久化提醒（替代旧 `ReminderScheduler`）

**状态视图 / 提示词组装（Step 3 重构后）**
- `state_view.py` ✅ — 不可变快照类型
- `state_view_builder.py` ✅ — 从各 store 拼出 StateView
- `state_serializer.py` ✅ — **纯函数**，StateView → prompt bytes
- `identity_file_manager.py` ✅ — soul.md / constitution.md I/O
- `soul_manager.py` ✅ — soul.md 版本控制（24h cooldown）
- `prompt_loader.py` ✅ — prompts/*.md 热重载

> 🗑 **已删除**：`prompt_builder.py`（被 StateSerializer 取代，CLAUDE.md 还残留此名）

**安全与权限**
- `authority_gate.py` ✅ — IGNORE/GUEST/TRUSTED/OWNER 四级
- `vital_guard.py` ✅ — Shell/文件写入前盾
- `shell_policy.py` ✅ — 目录 / sudo / 扩展名约束
- `shell_types.py` ✅
- `verifier.py` ✅ — 代码 / workspace 验证
- `credential_vault.py` ✅ — Fernet 加密的凭证库

**感知 / 环境**
- `vitals.py` ✅ — boot time / uptime / 桌面环境感知
- `maintenance_timer.py` ✅ — 周期维护（session 回收、记忆合并）
- `runtime_profiles.py` ✅ — 按场景定义可用工具集
- `model_config.py` ✅ — 模型绑定（persisted to `data/config/model_routing.json`）
- `group_filter.py` ✅ — QQ 群消息过滤
- `time_utils.py` ✅
- `trust_tagger.py` ✅ — 给来源打信任分（CLAUDE.md 没提）
- `minimax_vlm.py` ✅ — MiniMax VLM 调用封装（CLAUDE.md 没提）

**认证**
- `codex_oauth_client.py` ✅ — OpenAI/Codex PKCE 流程

**历史遗留**
- `channel_manager.py` ✅ — 多通道发送路由
- `phase0.py` ⚠ 早期阶段零的残留，可以核查是否还被引用

> 🗑 **在 CLAUDE.md 提到但实际已不存在**：`evolution.py`、`delegation.py`、`experience_skills.py`、`session.py`、`dispatcher/` 目录、`skills.py`（核心层的 skill 管理）、`heartbeat.py`、`browser_manager.py` 下的一些 Phase2 视觉旁路。这些词在 CLAUDE.md 的"Architecture"段落仍然出现，但代码里已经看不到了——**架构图需要重写**。

### 1.3 `src/agents/` — 代理框架（Phase 6）

| 文件 | 作用 | 状态 |
|------|------|------|
| `base.py` | `BaseAgent`，工具循环 + mutation_log 事件上报 | ✅ |
| `types.py` | `AgentSpec` / `AgentMessage` / `AgentResult` | ✅ |
| `registry.py` | dict-based 注册表 | ✅ |
| `coder.py` | Coder Agent（代码任务，workspace 隔离） | ✅ |
| `researcher.py` | Researcher Agent（搜索 + 浏览） | ✅ |
| `team_lead.py` | Team Lead（拆分任务 + 派发） | ✅ |

> CLAUDE.md 写 "There is NO agent dispatch layer" — **不冲突**。意思是：代理不是通过外部 dispatcher 调度，而是 Lapwing 通过 `delegate` / `delegate_to_agent` **工具调用**触发。代理本身完全活跃。

### 1.4 `src/tools/` — 工具层

16 个文件。所有能力都经过这里。

| 文件 | 负责的工具 | 状态 |
|------|-----------|------|
| `registry.py` | `ToolRegistry` + `build_default_tool_registry` | ✅ |
| `types.py` | `ToolSpec` / `ToolExecutionContext` / `Request` / `Result` | ✅ |
| `handlers.py` | 基础 shell / 文件 / 代码 / workspace 执行器 | ✅ |
| `tell_user.py` | **唯一的用户可见输出通道**（Step 5 核心约定） | ✅ |
| `commitments.py` | `commit_promise` / `fulfill_promise` / `abandon_promise` | ✅ |
| `shell_executor.py` | 安全 shell | ✅ |
| `file_editor.py` | 细粒度文件编辑 + 事务回滚 | ✅ |
| `code_runner.py` | 临时 Python 沙箱 | ✅ |
| `personal_tools.py` | time / send_message / send_image / read_image / search_web / fetch_url / delegate | ✅ |
| `memory_tools_v2.py` | recall / save_note / update_note / delete_note / search_notes / list_notes / get_fact / list_facts / get_interests | ✅ |
| `agent_tools.py` | `delegate` / `delegate_to_agent` | ✅ |
| `browser_tools.py` | 15+ 浏览器动作（gated by `BROWSER_ENABLED`） | ✅ |
| `research_tool.py` | 包一层 ResearchEngine | ✅ |
| `soul_tools.py` | `read_soul` / `edit_soul` | ✅ |
| `workspace_tools.py` | Coder 专用的 `data/agent_workspace/` 读写 | ✅ |

> `memory_tools_v2.py` 的 `v2` 后缀表明旧版已替换。代码中未找到 `memory_tools.py`（旧版已移除），命名只是历史痕迹。

### 1.5 `src/adapters/` — 消息通道

| 文件 | 作用 | 状态 |
|------|------|------|
| `base.py` | `BaseAdapter` ABC + `ChannelType`（QQ / DESKTOP） | ✅ |
| `qq_adapter.py` | OneBot v11 WebSocket（NapCat）| ✅ |
| `desktop_adapter.py` | 管理来自前端的 /ws/chat 连接池 | ✅ |
| `qq_group_context.py` | 群消息数据模型 | ✅ |
| `qq_group_filter.py` | `GroupEngagementDecider`（规则过滤） | ✅ |

> 🗑 `TelegramAdapter` 已不存在（CLAUDE.md 仍在 `src/app/` 下提到 TelegramApp，已过期）。

### 1.6 `src/api/routes/` — Desktop API

| 路由 | 端点 | 状态 |
|------|------|------|
| `chat_ws.py` | `/ws/chat` 主对话 WebSocket | ✅ |
| `auth.py` | `/api/auth/*` | ✅ |
| `browser.py` | `/api/browser/*` | ✅ |
| `agents.py` | `/api/agents/*` 非常精简（调度层已删，剩列表 / cancel） | ⚠ 部分方法是 stub |
| `identity.py` | `/api/v2/identity/*`（soul/voice/constitution CRUD + 历史 + 回滚） | ✅ |
| `life_v2.py` | `/api/v2/life/timeline` "她的生活" | ✅ |
| `status_v2.py` | `/api/v2/status` | ✅ |
| `system_v2.py` | `/api/v2/system/*`（CPU/内存/vitals） | ✅ |
| `tasks_v2.py` | `/api/v2/tasks/*`（只读） | ⚠ `messages` 子路由未接 mutation_log |
| `models_v2.py` | 运行时模型配置 | ✅ |
| `notes_v2.py` | `/api/v2/notes/*`（tree/read/search/recall） | ✅ |
| `permissions_v2.py` | `/api/v2/permissions` | ✅ |
| `events_v2.py` | `/api/v2/events` SSE 直接订阅 mutation_log | ✅ |

> CLAUDE.md 列的 `events_v2.py, life_v2.py, models_v2.py, notes_v2.py, permissions_v2.py, status_v2.py, system_v2.py, tasks_v2.py` 是对的，但多了一个 `identity.py`（没有 `_v2` 后缀但属于同代）。没有发现任何 v1-only 路由残留。

### 1.7 `src/memory/` — 记忆层（Step 2 v2.0 重构后）

| 文件 | 作用 | 状态 |
|------|------|------|
| `episodic_store.py` | **RAPTOR 下层**：按日 `YYYY-MM-DD.md` 的事件日志 | ✅ v2.0 |
| `episodic_extractor.py` | 对话后异步提取情景记忆 | ✅ v2.0 |
| `semantic_store.py` | **RAPTOR 上层**：`kevin.md` / `lapwing.md` / `world.md` 三类持久事实 | ✅ v2.0 |
| `semantic_distiller.py` | 每日维护：从多条情景提炼语义 | ✅ v2.0 |
| `working_set.py` | 跨层检索 → `MemorySnippets`（带 `[情景]`/`[知识]` 标签） | ✅ v2.0 |
| `vector_store.py` | ChromaDB 单 collection `lapwing_memory`（相似度+时效+信任+深度+访问） | ✅ |
| `embedding_worker.py` | 扫描 `embedding_version=="pending"` 的笔记补 embedding | ✅ |
| `note_store.py` | 文件型笔记（YAML frontmatter + Markdown） | ✅ |
| `compactor.py` | 对话窗口在 80% 处触发压缩 | ✅ |
| `conversation.py` | 历史 SQLite 模型（含若干**已废弃表**） | ⚠ 过渡 |

**🗑 废弃（架构层面，代码/数据仍在）**
- `user_facts` 表 — 被 SemanticStore 替代
- `interest_topics` 表 — 归并到 SemanticStore 的 `world` 分类
- `KEVIN.md` / `SELF.md` — 身份档案迁到 `data/identity/`
- `data/memory/notes/` — 已空，当前笔记走 `data/memory/episodic/` + vector store

（见 `docs/refactor_v2/step2_data_audit_notes.md` — "2026-04-18 记忆系统盘点报告"）

### 1.8 `src/heartbeat/` — 心跳

| 文件 | 作用 | 状态 |
|------|------|------|
| `proactive_filter.py` | 主动消息发送前质量闸（客服腔、堆叠口头语、列表化、>4 句 等） | ✅ |
| `actions/` | 空目录 | 🚧 占位 |

> CLAUDE.md 提到"12 个 action 文件"——**不准确**。实际心跳动作已经整合进 `src/core/inner_tick_scheduler.py` + `maintenance_timer.py`，老的 `src/heartbeat/actions/` 被清空。

### 1.9 `src/guards/`

| 文件 | 作用 | 状态 |
|------|------|------|
| `memory_guard.py` | 写入记忆前扫描注入 / 凭证 / 身份篡改 / 隐形 Unicode | ✅ |

> CLAUDE.md 提到的 `skill_guard.py` / `browser_guard.py` — 前者已不存在；`browser_guard.py` 迁到 `src/core/browser_manager.py` 内部（或已融入 `authority_gate`）。需要在后续盘点里确认。

### 1.10 `src/auth/`

| 文件 | 作用 | 状态 |
|------|------|------|
| `service.py` | `AuthManager` — OAuth、API session、bootstrap token、credential 解析 | ✅ |
| `storage.py` | `AuthStore` JSON 持久化 | ✅ |
| `openai_codex.py` | Codex PKCE provider | ✅ |
| `resolver.py` | `$secret:provider:field` 引用解析 | ✅ |
| `models.py` | `FailureKind` / `PurposeConfig` | ✅ |

### 1.11 `src/research/` — 研究子系统

| 文件 | 作用 | 状态 |
|------|------|------|
| `engine.py` | `ResearchEngine` 编排（scope_router → backend → fetcher → refiner） | ✅ |
| `scope_router.py` | 查询走 global / cn / both | ✅ |
| `fetcher.py` | httpx → 浏览器降级的 SPA-aware 抓取 | ✅ |
| `refiner.py` | LLM 汇总（answer / evidence / confidence / unclear） | ✅ |
| `prompts.py` | REFINE_PROMPT | ✅ |
| `types.py` | `ResearchResult` / `Evidence` | ✅ |
| `backends/` | Tavily / Bocha 等搜索后端 | ✅ |

### 1.12 `src/logging/` — 状态突变日志

| 文件 | 作用 | 状态 |
|------|------|------|
| `state_mutation_log.py` | **v2.0 唯一事实源**：SQLite + JSONL 双存。事件类型封闭枚举（ITERATION_*, LLM_*, TOOL_*, AGENT_*, PROMISE_*, TELL_USER, ATTENTION_CHANGE…） | ✅ |

### 1.13 `src/models/`

| 文件 | 作用 |
|------|------|
| `RichMessage` | 通道间共享的富消息结构 |

### 1.14 `src/utils/`

小工具集合（未逐个确认；不属于设计关键路径）。

---

## 2. 数据目录 `data/`

```
data/
├── identity/         ✅ soul.md / constitution.md（Lapwing 不可写）
├── memory/
│   ├── episodic/     ✅ v2.0 情景层（按日）
│   ├── semantic/     ✅ v2.0 语义层（kevin/lapwing/world）
│   ├── conversations/summaries/  ✅ 压缩摘要（92 个）
│   └── notes/        🗑 已空，保留目录
├── chroma/           ✅ ChromaDB 向量库（主）
├── chroma_memory/    ⚠ 可能是旧向量库目录，需核查是否仍有写入
├── config/
│   ├── model_routing.json        ✅
│   ├── permissions.json          ✅
│   └── permission_overrides.json ✅
├── credentials/      ✅ 凭证库（vault.enc，Fernet 加密）
├── browser/
│   ├── profile/      ✅ Playwright 持久 context
│   ├── screenshots/  ✅
│   └── state.json    ✅
├── agent_workspace/  ✅ Coder Agent 隔离沙箱
├── workspace/        ⚠ 和 agent_workspace 是否重复？
├── tasks/            ✅ TaskFlow checkpoint
├── tool_results/     ✅ 工具输出缓存
├── consciousness/    ✅ inner tick 产物
├── backups/          ✅ VitalGuard 自动备份
├── logs/             ✅ lapwing.log + mutations_YYYY-MM-DD.log
├── lapwing.db*       ✅ 主 SQLite（WAL）
├── mutation_log.db*  ✅ 突变日志 SQLite（WAL）
├── vitals.json       ✅ 启停状态
└── lapwing.pid       ✅ 进程锁
```

> ⚠ 待核查项：
> - `data/chroma_memory/` vs `data/chroma/` 是否并存
> - `data/workspace/` vs `data/agent_workspace/` 是否重复
> - `data/evolution/` — CLAUDE.md 说存在，但**磁盘上没有**（v2.0 起记忆/进化合并了）
>
> 🗑 `data/evolution/rules.md`, `interests.md`, `changelog.md` — CLAUDE.md 的这段描述已**整体过期**。

---

## 3. 前端

### 3.1 `desktop-v2/` — 活跃前端 ✅

Tauri v2 + React 19 + TypeScript + Zustand + shadcn/ui + Tailwind 4 + CodeMirror + Recharts。

**实际页面**（`src/pages/`）：
- `ChatPage.tsx` — 主对话
- `IdentityPage.tsx` — 身份文档（soul / voice / constitution）编辑
- `NotesPage.tsx` — 记忆浏览 + CRUD
- `SystemPage.tsx` — 仪表盘（心跳 / 提醒 / 资源环）
- `StatusDetailPage.tsx` — 系统指标详情
- `SettingsPage.tsx` — 设置

> ⚠ CLAUDE.md 写的 `DashboardPage / MemoryPage / ModelRoutingPage / PersonaPage / SensingPage / TaskCenterPage` **名字已全部对不上**。实际命名更简洁，功能合并了（例：MemoryPage→NotesPage, PersonaPage→IdentityPage, DashboardPage+SensingPage→SystemPage）。

**components 分类**：chat / layout / memory / model-routing / settings / shared / status / tasks / ui(16 个 shadcn)
**stores**：`chat.ts` / `server.ts`
**hooks**：`useSSE.ts` / `useWebSocket.ts`
**types**：`api.ts` / `chat.ts` / `sensing.ts` / `tasks.ts`

### 3.2 `desktop/` — 🗑 废弃

Tauri v1 + React 18。~3.2K 行，最近一次有意义的提交停在 3 月。
CLAUDE.md 已明确 "legacy"。不再被 desktop-v2 引用。**建议归档到 `docs/archive/desktop-v1/` 或单独分支保留**。

---

## 4. 提示词 `prompts/`

全部 Markdown，热重载：

| 文件 | 用途 |
|------|------|
| `lapwing_soul.md` | 核心身份 |
| `lapwing_voice.md` | 语气（✕/✓ 对照） |
| `agent_coder.md` | Coder 代理系统提示 |
| `agent_researcher.md` | Researcher 代理系统提示 |
| `episodic_extract.md` | 情景记忆抽取指令 |
| `semantic_distill.md` | 语义蒸馏指令 |
| `compaction.md` | 对话压缩指令 |
| `browser_vision_describe.md` | 视觉模型描述网页 |
| `group_engage_decision.md` | QQ 群插话决策 |
| `life_today_tone.md` | "我今天怎么样"一句话状态 |
| `README.md` | 索引 |

---

## 5. 工具与测试

### 5.1 `scripts/`

| 文件 | 类型 |
|------|------|
| `deploy.sh` | ✅ 官方部署（取代 nohup） |
| `setup_browser.sh` | ✅ Playwright 初始化 |
| `smoke_test_step5.py` | ✅ Step 5 冒烟 |
| `migrate_to_trajectory.py` | ⚠ Step 2 迁移（已完成） |
| `verify_dual_write.py` | ⚠ 双写核对（过渡期工具，可清理） |
| `drop_sessions_table.py` | ⚠ 一次性，可清理 |
| `diagnose_schedule.py` | ✅ |
| `qq_export.py` | ✅ |
| `test_codex_oauth.py` | ✅ |
| `migrations/` | 有序 SQL 迁移 |

### 5.2 `tests/`

~140 文件、1072 条用例、**100% 通过**（Step 3 后）。按 src/ 结构镜像：core(48) / tools(13) / memory(12) / integration(12) / api(10) / agents(8) / research(7) / logging(6) / app(5) / auth(4) / adapters(3) / baseline_v2(3) / experiments+scripts+guards。

---

## 6. 配置 `config/`

| 文件 | 状态 |
|------|------|
| `.env` | ✅（未入库，但 `.gitignore` 需复查） |
| `.env.example` | ✅ |
| `.env.test` | ✅ |
| `settings.py` | ✅（18.3K，所有变量 `os.getenv`） |

**Feature flags**：`SKILLS_ENABLED`、`MEMORY_CRUD_ENABLED`、`AUTO_MEMORY_EXTRACT_ENABLED`、`SELF_SCHEDULE_ENABLED`、`SESSION_ENABLED`、`EXPERIENCE_SKILLS_ENABLED`、`QQ_ENABLED`、`BROWSER_ENABLED`、`BROWSE_ENABLED`、`LOOP_DETECTION_ENABLED`、`SHELL_ENABLED`、`DELEGATION_ENABLED`、`MESSAGE_SPLIT_ENABLED`、`CHAT_WEB_TOOLS_ENABLED`。

> ⚠ 安全遗留（`HEALTH_CHECK_REPORT.md` R-01）：`data/config/model_routing.json` 明文存 API key，建议改用 vault 或环境变量引用 `$secret:...`。

---

## 7. 其他

### 7.1 `skills/` — 🚧 经验技能库

```
skills/
├── general/             16 个 INC-20260412 / 0413 / 0414 文件
├── coding/ content/ daily/ research/ system/   空
├── _index.json          0 active skills
└── _registry.json
```

都是从真实对话里萃取的 incident 模板。尚无自动调用闭环，仅作候选语料。**状态：实验期**。

### 7.2 `skill_traces/` — ⚠ 归档

1000+ JSON trace。只写不读，用于审计。

### 7.3 `watchdog/`

| 文件 | 作用 |
|------|------|
| `sentinel.py` | 独立看门狗进程 |
| `lapwing-sentinel.service` | systemd 单元 |

### 7.4 `docker/`

`sandbox/` 子目录，辅助隔离测试，主部署仍在 PVE 裸机。

### 7.5 `docs/`

- `archive/` — 设计稿 / 决策记录 / 3 月冲刺结案
- `refactor_v2/` — **Blueprint v2.0 逐步执行日志**（本次结构盘点的重要依据）
  - `cleanup_report_step{1,3,4,6,7}.md`
  - `step2_data_audit_notes.md`
  - `step4_decisions.md`、`step6_teamlead_design.md`
  - `logging_design.md`、`memory_naming_conventions.md`、`backup_conventions.md`
- `superpowers/` — Claude Code skill 参考（如果存在）
- `diagnosis_dodgers_20260416.md` / `20260417.md` — 排障记录
- `lapwing-frontend-blueprint.md`、`lapwing-restructure-final.md` — 架构蓝图

### 7.6 `logs/`

运行日志聚合。与 `data/logs/` 分工：项目根 `logs/` 是服务级滚动日志，`data/logs/` 是业务 / mutation 细粒度日志。

---

## 附录 A：CLAUDE.md 与现状的已知偏差（建议下次修订）

| CLAUDE.md 的说法 | 代码现状 |
|------------------|----------|
| 提到 `PromptBuilder.build_system_prompt()` 8 层 | 🗑 `PromptBuilder` 已被 `StateViewBuilder + StateSerializer` 取代（Step 3） |
| 提到 `ExperienceSkillManager.retrieve()` | 🗑 该类已不存在；相关能力暂存 `skills/` 目录，未自动化 |
| 提到 `SessionManager.resolve_session()` | 🗑 Session 模型已由 `TrajectoryStore + Attention` 替代 |
| 提到 `src/core/evolution.py`、`delegation.py`、`session.py`、`heartbeat.py` | 🗑 文件均已不在；能力已拆分或移除 |
| 提到 `src/heartbeat/actions/`（12 个文件）| 🚧 目录存在但已空；动作合并到 `inner_tick_scheduler + maintenance_timer` |
| 提到 `src/app/TelegramApp` | 🗑 Telegram 通道整体移除 |
| 提到 `data/evolution/rules.md / interests.md / changelog.md` | 🗑 路径不存在 |
| 提到 desktop-v2 页面 `DashboardPage / MemoryPage / ModelRoutingPage / PersonaPage / SensingPage / TaskCenterPage` | 实际只有 Chat / Identity / Notes / System / StatusDetail / Settings 六页 |
| 没提：`TrajectoryStore` / `StateMutationLog` / `Attention` / `MainLoop` / `EventQueue` / `InnerTickScheduler` / `CommitmentStore` / `DurableScheduler` / `BaseAgent` 家族 / `trust_tagger` / `minimax_vlm` | ✅ 全是 v2.0 新的核心模块，需补入 |

---

## 附录 B：重构进度快照

| Step | 内容 | 状态 |
|------|------|------|
| 1 | 引入 `StateMutationLog`，删除老 `EventLogger` 与死代码 | ✅ |
| 2 | 双写 trajectory，收口为单一事实源；废弃 user_facts / interest_topics | ✅ |
| 3 | 退役 `PromptBuilder`，启用 `StateViewBuilder + StateSerializer` | ✅ |
| 4 | 单消费者 `MainLoop` 骨架 | ⚠ 骨架在，handler 仍为 TODO |
| 5 | `tell_user` 单出口 + Commitment Reviewer | ⚠ 工具已在，reviewer 未落地 |
| 6 | Team Lead 多代理 | ✅ 框架就位，打磨中 |
| 7 | RAPTOR 双层记忆 + 维护任务 | ✅ 结构完整，每日蒸馏待验证 |

---

**文档作者**：Claude（本次盘点）
**生成时间**：2026-04-19
**覆盖范围**：`/home/kevin/lapwing/` 全量（除 `venv/`、`__pycache__/`、前端 `node_modules/`）
