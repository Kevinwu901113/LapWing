[pytest]
asyncio_mode = auto
markers =
    eval: mark test as regression eval (requires --run-evals)
    integration: requires local services (ChromaDB, SQLite with real data)
    e2e: full system test requiring multiple subsystems
    requires_llm: requires a real LLM API key
    requires_browser: requires Playwright/browser
    requires_network: requires external network access
